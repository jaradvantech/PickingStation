<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <!-- RBS Add theme for splash screen-->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

   <!-- RBS Custom sizes for drawer (rescaled screen makes default look too small)-->
    <style name="NavigationDrawerStyle">
        <item name="android:textSize">20dp</item><!-- text size in menu-->
        <item name="android:listPreferredItemHeightSmall">65dp</item><!-- item size in menu-->
    </style>

    <!-- RBS Custom progress bar (for Line status)-->
    <style name="CustomProgressBarHorizontal" parent="android:Widget.ProgressBar.Horizontal">
        <item name="android:progressDrawable">@drawable/pallet_progress_bar</item>
        <item name="android:minHeight">10dip</item>
        <item name="android:maxHeight">20dip</item>
    </style>

    <!-- RBS Custom SeekBar sliders for box position in pallet creator-->
    <style name="CustomSliderBar" parent="android:Widget.SeekBar">
        <item name="android:progressDrawable">@drawable/position_slider</item>
        <item name="android:minHeight">10dp</item>
        <item name="android:maxHeight">10dp</item>
    </style>

    <style name="list_item_appearance">
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:colorControlActivated">@color/colorSecondary</item>
        <item name="android:colorControlHighlight">@color/colorSecondary</item>
        <item name="android:colorControlNormal">@color/colorBlack</item>
        <item name="android:textColorHighlight">@color/colorSecondary</item>
        <item name="android:color">@color/colorSecondary</item>
    </style>

</resources>
